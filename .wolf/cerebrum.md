# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-08-30

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->

## Key Learnings

<!-- Project-specific conventions discovered during development. -->
- `openwolf init` has a stale copy-list bug: `copyHookScripts` copies a fixed 13-file list of hooks from `dist/src/hooks/` into `.wolf/hooks/`, but the shipped `pre-read.js`/`pre-write.js` import `../specs/spec-store.js` + `../specs/inject.js` and init never copies the `specs/` runtime. Result: a fresh init's SDD spec-context injection + TDD reminder throw MODULE_NOT_FOUND (caught → fail open, silent). Fix: copy `openwolf`'s `dist/hooks/specs/*.js` into `.wolf/specs/` (NOT `.wolf/hooks/specs/` — the import is `../specs/` from `.wolf/hooks/`). The `specs/` runtime is self-contained (only `node:*` + `./types.js`). This repo is fixed as of 2026-08-30.
- `doc_ai/` holds cross-module AI/design/process docs (GODOT_LLM_DSL_DESIGN.md, SOP_TDD_AI_TESTING.md, PLAN_LLM_DSL_IMPL.md); `docs/` is reserved for the 144 module teaching docs (INDEX.md + DOC_SPEC.md).
- A new scripting language plugs into Godot C++ via 3 routes: `ScriptLanguage` (ScriptServer), GDExtension native (`object_method_bind_ptrcall`), or declarative `.tscn`/`.tres` (`ResourceFormatLoaderText`). No VM is required by the `ScriptInstance` interface.
- Standalone fast TDD harness for pure C++: `gdsl/test.ps1` sources vcvars64.bat, compiles with `cl /utf-8 /EHsc /std:c++17`, links doctest single header, runs in seconds (no engine rebuild).
- GDExtension ABI has **no** `object_emit_signal` (grep `core/extension/gdextension_interface.json` → zero hits). Emitting a signal from raw GDExtension C requires `classdb_get_method_bind("Object","emit_signal", hash)` + `object_method_bind_call`, and that hash must be exact: `ClassDB::get_method_with_compatibility` (`class_db.cpp:1166-1198`) returns nullptr unless `MethodBind::get_hash()` matches, and that hash = `MethodInfo::get_compatibility_hash()` (`method_bind.cpp:35-45`). The hash is name-independent and purely structural, and can be **computed from source** (port `hash_murmur3_one_32`+`hash_fmix32` from `core/templates/hashfuncs.h:112-122,144-150` + `get_compatibility_hash` from `core/object/method_info.cpp:86-116`) instead of requiring an `extension_api.json` dump. **CRITICAL correction (2026-08-31, real-machine verified)**: `Object::emit_signal` **has a return value (Error)** — the hand-written `mi` in `object.cpp:1866` omits return_val, but `create_vararg_method_bind` derives return type INT + class_name "Error" from the C++ signature (`type_info.h:247-256` MAKE_ENUM_TYPE_INFO). Omitting return_val produces the wrong hash. Correct hash = **4047867050 (0xF1458CAA)**, verified against real-machine `--dump-extension-api`. The older value 2866548813 (0xAADC104D) was wrong (treated emit_signal as no-return).
- Godot 4.7 ABI has **no** `variant_new_string_name`/`variant_new_string` (only `variant_new_copy`/`variant_new_nil` + generic `variant_construct`). To build a typed Variant from a raw builtin value use `get_variant_from_type_constructor(GDExtensionVariantType)` → `GDExtensionVariantFromTypeConstructorFunc(void(*)(GDExtensionUninitializedVariantPtr, GDExtensionTypePtr))` (wraps `VariantTypeConstructor<T>::variant_from_type`, `variant_internal.h:1002-1007`). `GDExtensionVariantType::STRING_NAME` = 21 (matches `Variant::Type`, `variant.h:96-124`).
- Godot 4.7 ABI has **no** `string_name_destroy` (only `string_name_new_with_latin1_chars`/`_utf8_`). StringName is refcounted + intern-table (`string_name.cpp:111-129` unref frees when refcount→0). **`p_is_static` does NOT change buffer ownership** — `StringName(const char*, bool p_static)` always `_data->name = p_name` (copies via `String::operator=` → `append_latin1`, `ustring.h:688`); `p_static` only sets `static_count` (`string_name.cpp:232,245`) used for leak reporting at cleanup. For compile-time literals, create the StringName **once** and cache it — per-call static StringName creation would leak `static_count` unboundedly (`string_name.cpp:231-232` increments static_count per static creation).
- `GDExtensionPropertyInfo` must NOT carry a NULL `hint_string` (nor NULL name/class_name): `PropertyInfo(const GDExtensionPropertyInfo&)` (`core/object/property_info.h:163-170`) unconditionally dereferences all three via `*reinterpret_cast<...*>`. NULL `hint_string` → crash at method/property registration time. Use a zero-initialized `static uint8_t empty_string[8]` (empty String, COW-null-safe) as the placeholder.
- `sizeof(Variant)` = 24 bytes (single-precision/`real_t=float`) or 40 (double), per `variant.h:162`; GDExtension assumes single precision → 24-byte 8-aligned holder.
- To syntax-check generated GDExtension C without a full engine build: hand-write a stub `gdextension_interface.h` with the real type/field names from the json, then `cl /c` the generated `.c` against it.
- GDExtension ABI has **no** `object_get_instance` (only `object_set_instance`, grep `gdextension_interface.json` → zero `object_get_instance`). The engine supplies `self` to a registered method via `p_instance` = `Object::_get_extension_instance()` (`gdextension.cpp:106`, `object.h:525`). To retrieve a *different* object's C struct (e.g. a collision `target`), the ONLY path is the instance-binding API: `object_set_instance_binding`/`object_get_instance_binding` keyed by the library token (`object.cpp:2153,2167`). A NULL `free_callback` in `GDExtensionInstanceBindingCallbacks` is safe (memory then freed by the extension's `free_instance_func`), avoiding double-free.
- `GDExtensionClassMethodInfo` requires `arguments_info` and `arguments_metadata` arrays of size `argument_count` whenever `argument_count > 0` (`gdextension.cpp:211-213` dereferences both). For a single OBJECT argument: `GDExtensionPropertyInfo{ type=GDEXTENSION_VARIANT_TYPE_OBJECT(=24), name="target", class_name=<TargetType>, hint=0, hint_string=NULL, usage=0 }` + metadata `GDEXTENSION_METHOD_ARGUMENT_METADATA_NONE(=0)`.
- To extract an Object from a Variant (in `call_func`): `get_variant_to_type_constructor(GDEXTENSION_VARIANT_TYPE_OBJECT)` → `void(*)(GDExtensionUninitializedTypePtr r_dest, GDExtensionVariantPtr)` fills a `GDExtensionObjectPtr`. In `ptrcall_func`, an OBJECT argument arrives as `*(GDExtensionObjectPtr *)p_args[0]` (a pointer to the object pointer).

## Do-Not-Repeat

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->
- [2026-08-29] MSVC reads source as GBK(936) by default; UTF-8 Chinese comments make declarations mis-parse (C2039 "not a member"). Compile with `/utf-8`.
- [2026-08-31] Chasing a teardown-stage segfault, I changed signal-name StringName `p_is_static` true→false and wrote a comment claiming "true reuses the DLL literal buffer → cleanup dangling". Both were wrong: `string_name.cpp` shows StringName always copies the name and `p_static` only affects `static_count`. The change also broke `test_codegen.cpp` (still asserted `true` → 82/83 red, contradicting a "all green" report). Do NOT change `p_is_static` to chase the exit segfault, and do NOT claim a teardown crash's root cause without a symbolicated stack. When a real-machine crash is reported, re-run `gdsl/test.ps1` before claiming green.

- [2026-09-01] Claimed "scene connection dangling-reference check is missing" without reading `scene_json.cpp` — it already exists (`scene_json.cpp:173-193`, FR-004). The audit's "3 gaps" was really 2: #4 (literal/type coercion in `typecheck.cpp`) and #6 (ontology not derived from `extension_api.json`) are real; #8 (dangling ref) was already implemented. Do NOT list a validation gap until you've opened the validator source — declare gaps from the file, not from memory/docs.

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->
- D8 (SDD system choice, 2026-08-30): spec-driven dev runs on **OpenWolf SDD** (in-repo `.wolf/` scaffolding + `openwolf spec` CLI), not GitHub Spec Kit (`specify` CLI). Rationale: on the test-enforcement axis OpenWolf has running hooks (post-test observes failures, pre-read/pre-write inject TDD reminders, tasks-template has hard phase gates) vs Spec Kit's zero-runtime "tests are OPTIONAL" templates. Spec Kit was never initialized in this repo (nothing to remove); the global `specify` CLI is left installed by explicit user choice.
- D9 (SDD gate promotion, 2026-08-30): SOP §4.4 DoD promoted from doc to a hard gate, both ways — (a) the `/implement` command (all 4 agent dirs) now refuses to check a task box without pasted green `--test` output; (b) `.wolf/hooks/post-test.js` marks the active spec `blocked` when a test fails during the `implement` phase. Known limit: `openwolf spec next` itself does not refuse on `blocked` (would need patching the global binary, lost on `openwolf update`), so (b) is a durable marker and (a) is the enforcement.
- D10 (retro-spec granularity, 2026-08-30): the DSL compiler is spec'd one spec per component — 8 retro-specs (001-json … 008-codegen-logic) mapping each gdsl translation unit's seam to its existing test file, plus 1 forward spec (009-engine-integration = S6) at a falsifiable scene-load bar. `doc_ai/` left untouched as the design archive.
- D1: DSL compiler kernel lives in standalone `gdsl/` with its own doctest harness (seconds per red-green); engine module integration deferred to slice S6. Rationale: cycle-time (SOP §1.1) beats test-convention consistency for pure functions.
- D2: first slice is the logic parser (`.gdsl` → AST), not the declarative JSON→tscn layer. User chose parser-first over my "declarative tracer bullet" recommendation.
- D4 (S6.2a): effect ontology v1 = `set_field` (`ref.field = / += / -= literal`) + single-comparison guard (`ref.field <cmp> literal`), `ref` ∈ {self, bare}. Rules compile to methods via `classdb_register_extension_class_method` (call_func + ptrcall_func). `emit_signal` deferred to S6.2b, cross-participant `target` deferred to S6.2c (explicitly rejected, not silently ignored).
- D5 (S6.2b): `emit_signal` via `classdb_get_method_bind("Object","emit_signal",hash)` + `object_method_bind_call`. Hash **computed from source** (murmur3/fmix port in `gdsl/abihash.h`) = 2866548813, not hand-invented — pins the "name-independent structural hash" fact and removes the need for an `extension_api.json` dump. Variant marshalling via `get_variant_from_type_constructor(STRING_NAME)` (4.7 has no `variant_new_string_name`). Signal names cached as static StringNames (no `string_name_destroy` in ABI → per-call creation would leak).
- D6 (S6.2c): `target` type is an explicit clause `rule <Name> by <Owner> [target <Type>]:`. Rejected "default target = by type" (equals self, pointless) and "fixed Node" (loses type safety). `by` remains the rule owner (self); referencing `target.<field>` without a `target` clause is a typecheck error.
- D7 (S6.2c): cross-object C-struct retrieval uses instance binding (`object_set_instance_binding`/`object_get_instance_binding`, token-keyed), since the ABI has no `object_get_instance`. `free_callback` = NULL (memory owned by `free_instance_func`). Rule methods with a target register `argument_count=1` + OBJECT `arguments_info`.
- D11 (S6.3, 2026-08-31): emit_signal compat hash corrected to 4047867050 (0xF1458CAA) after real-machine `--dump-extension-api` revealed `emit_signal` returns Error (INT + class_name "Error"), invalidating the source-derived 2866548813 that treated it as no-return. The hash is still computed from source (now including has_return + return type + return class_name), not hand-invented.
- D12 (S6.3, 2026-08-31): `GDExtensionPropertyInfo.hint_string` must point to a zero-initialized `gdsl_empty_string[8]`, never NULL — `PropertyInfo(const GDExtensionPropertyInfo&)` dereferences it unconditionally.
- D13 (Gap A, 2026-09-01): typecheck coercion policy v1 = no implicit coercion; only int literal → float field (widening); Named fields accept only `null`; bool only `true`/`false`; string fields not in rule layer. Enforced by `literal_matches` in `gdsl/typecheck.cpp` (replaces existence-only `has_field` with `find_field` + type check). 88/350 green.
- D14 (Direction 5, 2026-09-01): String fields go through hot-reload **preserve** (not drop-to-default), with a documented bounded leak. Rationale: keep String behavior consistent with int/float/bool/Named (all preserve); dropping would silently lose String state. The leak = **one transient String per getter + one per setter, per reload, per String field** — because the 4.7 ABI has **no `string_destroy`** (grep `gdextension_interface.json` `interface` → only `variant_destroy`/`object_destroy`), and `VariantTypeConstructor<String>` is `memnew_placement` copy/placement-new (`core/variant/variant_internal.h:990-1006`). Dev-time only, tens of bytes, treated as an ABI wall (like `variant_new_string_name`/`object_get_instance`), NOT engineered around. **Real-machine verified 2026-09-01**: isolated project `gdsl/example/string_test/` (class `Hero`, `nickname: string`), `Hero.new()` → set `nickname="aria"` → `reload_extension("res://string_fields.gdextension")` → `after reload: nickname=aria nick_same=(true)` (3/3 deterministic, exit 0, `p.free()` no ObjectDB leak → field buffer freed correctly by destructor).
- **Real-machine test pattern (reuse)**: separate isolated project dir (not the main `example/` which has 4 extensions that all register `Player` — Godot auto-loads all `.gdextension` at project root, first wins, so a shared class name collides). Use a UNIQUE class name (`Hero` not `Player`) + its own `project.godot` + `@tool` autoload that does `Object.new()` → set fields → `Engine.get_singleton("GDExtensionManager").reload_extension("res://<name>.gdextension")` → print before/after → `get_tree().quit()`. Run with `.godot-bin/Godot_v4.7-rc3_win64_console.exe --editor --headless --path <dir>`. Note: `ObjectDB instance was leaked at exit` warnings = the test's own `new()` not freed; add `p.free()`.
- D15 (Direction 5, 2026-09-01): String field uses clean ownership — constructor copies the default via `gdsl_mem_alloc(sizeof(lit))+memcpy` (a literal would be read-only and crash on setter/free), destructor `gdsl_mem_free`, setter frees-old-then-allocates-new. Only the transient getter/setter String leaks (ABI wall); the field buffer itself is leak-free. String default no longer double-quoted (was `self->name = ""hero"";` — a C syntax error) because `parse_state_field` stores `default_value` WITH quotes, so `default_literal` must NOT wrap again.
- D16 (Direction 5, 2026-09-01): manifest consistency — every `.gdextension` that loads a gdsl-generated DLL **must** declare `reloadable = true` (else the engine disables instance tracking and the shutdown-segfault fix via `_clear_extension` on class unregister never engages). Fixed `target_only.gdextension` which was missing it (named/self_rule already had it). All 3 now consistent: `reloadable=true`, `entry_symbol="gdsl_library_init"`, `windows.x86_64` matches the DLL.
- D17 (Gap B, 2026-09-01): `@extends <base>` validated at typecheck against the ClassDB class list — vendored as a **generated** `gdsl/godot_classes.h` (sorted C array + binary search `is_godot_class`), regenerated by `gdsl/gen_godot_classes.py` from the repo-root `extension_api.json`. NOT read at runtime: parsing a 7MB json on every gdslc/doctest typecheck call would blow the fast harness cycle. v1 validates existence only (not `is_instantiable`, to avoid rejecting abstract-but-legal bases like Node/Object/RefCounted); `@extends <GDSLtype>` is correctly rejected (GDSL types are not Godot bases). Catches hallucinated/typo'd bases (`Node2Dd`, `FooBar`) as "compile error → fix" instead of a runtime `classdb_construct_object` crash.
- D18 (010 LLM convergence, 2026-09-01): measured "LLM-friendly" empirically with a cold-test harness — 8/8 first-try valid (4 recipe tasks × few-shot K∈{0,1}), mean iterations-to-valid = 1.0, few-shot sensitivity = 0 (K=0 and K=1 both 1.00), semantic on-target verified for all 4. Bar = gdslc parse+typecheck pass (compile-valid), NOT runtime-correct (that's spec 009). Model was `deepseek-v4-flash-vision-exp` (the user's `claude` CLI routes to it), not Claude itself — so this measures "this model can author valid gdsl zero-example", a strong signal the language is self-explanatory on the compile axis, but NOT a proof weaker models can, nor that gdsl can express complex games. Still needed to close Era 16's claim: playtest feedback loop (FR-011) to measure "what does a correct recipe actually run as".
- D19 (010-Expand + FR-011 playtest, 2026-09-01): A) expanded 010 to 8 tasks (4 easy + 4 composite: multi-rule, float+emit, string+rule, two-types-cross) × K{0,1} → **16/16 first-try valid**, composition does NOT break LLM-friendliness (for this model). B) built `gdsl/toolchain/playtest.py` (full pipeline `.gdsl → gdslc → .c → cl /LD → .dll → .gdextension → engine` + GDScript assert) and **verified 3 recipes runtime-correct**: basic_int (hp 3→2), float_accel (speed 300.4→305.4), target_emit (cross-participant p.hp 3→2 via instance binding). This closes the "compile-valid ≠ runtime-correct" gap for the rule-execution path — gdsl recipes actually run as intended in-engine. Remaining half (wiring playtest result back into the LLM loop to close the full ScriptDoctor-style cycle) not yet done.

## Playtest (FR-011) facts

- **`cl /LD` from Python on Windows**: shell out to a temp `.ps1` that does `cmd /c "\"$vcvars\" >nul 2>&1 && cl /nologo /utf-8 /LD /I<toolchain> <c> /Fe:<dll> /Fo:<dll>.obj"`. Paths have no spaces here → keep cl args **bare** (no inner `"`), or PowerShell truncates the string at the inner quote → "TerminatorExpectedAtEndOfString". The only quoted token is `` `"$vcvars`" ``.
- **subprocess text decoding**: `cl`/engine output mixes UTF-16 (vcvars banner), GBK, and UTF-8 → always pass `text=True, encoding="utf-8", errors="replace"`, else `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xcb` and stdout becomes None.
- **playtest pattern**: isolated project dir (no clash with other extensions), `@tool` autoload doing `Type.new()` → call rule method (snake_case of rule name) → `print` + `get_tree().quit()`. Rule methods are the registered names (e.g. `take_damage`, `accel`, `on_hit`). Engine run: `Godot_v4.7-rc3_win64_console.exe --editor --headless --path <dir>`. Cross-participant rule called as `owner.method(target_object)`, and the rule's C resolves the target via `object_get_instance_binding`.
- D20 (FR-011 closed loop, 2026-09-01): wired playtest into the LLM loop — `gdsl/toolchain/loop_bench.py` `converge()`: LLM → gdslc compile → auto-generate playtest `.gd` (`gdsl_playgen.py` parses type/rule/guard/effect from the recipe) → in-engine run → `RESULT ALLPASS` = converged, else feed FAIL lines back. Ran basic_int + float_accel at K=0 → **both converge in 1 cycle** (compile_failures=0, playtest_failures=0). IMPORTANT honest limit: the auto-playtest **forces the guard true** (sets the guard field to satisfy the comparison), so it asserts only that the rule's OWN effect executes in C — a codegen/execution validator, NOT a semantic-vs-prose-spec checker (that needs per-task expected outcomes). FAIL→fix fires only on genuine runtime bugs (none in current correct codegen).
- D21 (Direction 2 semantic-vs-spec golden, 2026-09-01): built `gdsl/toolchain/golden.py` — a per-task GOLDEN reference `{rule, setup_owner/setup_target, expect_owner/expect_target}` (human-defined "correct behavior") that the playtest asserts against, instead of against the recipe's own effect (circular). **Proven**: correct basic_int recipe → `RESULT ALLPASS`; deliberately-wrong recipe (`+=1` instead of `-=1`, compile-valid but semantically reversed) → `FAIL Player.hp expected 4 got 6` + `RESULT SOME_FAIL` — **the reversal is caught**. Wired into `loop_bench.run_playtest`: if the task has a golden, use it; else fall back to the effect-based check. This is the layer that catches "LLM wrote the rule backwards" that playgen's effect-based check cannot. The goldens are authored by me (from the task specs I wrote) and should be reviewed/corrected by the human.
- **Gotcha (generated-GDScript hang)**: an engine run timing out at 240s was NOT the engine — my generated GDScript redeclared `var owner`/`var scenario_ok` per scenario in the same `_ready()` → GDScript "already declared" parse error → autoload's `_ready` never runs → never quit → timeout. Fix: declare the var ONCE, then assign per scenario (`owner = X.new()`). Lesson: static-check generated GDScript for `var` redeclarations before burning a 240s engine run.
- D22 (Direction 2 expanded + semfix loop, 2026-09-01): authored goldens for ALL 8 tasks (incl. `string_name` with a no-rule `__default__` scenario asserting default state; multi_rule_self/two_types_cross with 2 scenarios each). **Verified via engine** correct recipes → ALLPASS for basic_int, float_accel, target_emit (and the rest were being re-verified once a GDScript generator bug was fixed). **Semantic-fix loop PROVEN** (`gdsl/toolchain/semfix_loop.py`): a deliberately-reversed recipe (`+=1` instead of `-=1`, compile-valid) → golden `FAIL Player.hp expected 4 got 6` → fed that FAIL to the LLM → LLM flipped it to `-=1` → golden `ALLPASS` → `SEMFIX LOOP PASSED`. This is the last missing half of FR-011: a wrong-but-valid recipe is caught AND the LLM fixes from the semantic feedback.
- **Gotcha (string literal in generated GDScript)**: the golden value rendering `_gv("hero")` must emit the value AS a GDScript literal (`"hero"`, with quotes) for the assert, and the FAIL **print message** must NOT embed the value inside a double-quoted GDScript string (unescaped inner `"` → parse error → autoload never runs → hang). Fix: comma-separated `print("FAIL", "X.y", "expected", "hero", "got", str(...))` — values as separate args, no escaping. Lesson: when generating GDScript from arbitrary values, keep values as separate print args, never interpolate into a quoted string.
- D23 (009 scene-load acceptance, 2026-09-01): built `gdsl/toolchain/scene_accept.py` — recipe → DLL → auto-gen `.tscn` (one node per recipe `type`) → engine `load(tscn).instantiate() + add_child` → assert node count / native class / default state / a rule method firing in the scene tree. **Verified in-engine**: basic_int → CHILD_COUNT=1, NODE0_CLASS=Player, hp=3, take_damage RULE_APPLIED=true, ACCEPT; two_types_cross → CHILD_COUNT=2 (Bullet+Player), defaults correct, fade RULE_APPLIED=true, ACCEPT. This is FR-003's foundational bar — a recipe's types instantiate & behave in a REAL Godot scene (unlike playtest.py's isolated `new()`). NOT yet: TC-5 cross-entity (target rule through the scene's node graph), TC-6 signal observable, TC-7 lifecycle, TC-8 game trajectory, and the full DECLARATIVE `scene_from_json(JSON→tscn)` path (scene_accept auto-generates a simple tscn from the recipe's types, not a JSON scene spec with connections/positions).
- D24 (009 TC-5 cross-entity + TC-6 signal observable, 2026-09-01): TC-5 — scene_accept now tests the TARGET rule in-scene: two_types_cross `c0(Bullet).on_hit(c1(Player))` fires in the scene tree → `TARGET_RULE Player.hp before=5 after=4 applied=true`, ACCEPT. TC-6 — `codegen_logic.cpp` adds `classdb_register_extension_class_signal` (cache + load + per-class registration of each `emit(<sig>)` as a zero-arg signal); engine-verified `HAS_SIGNAL=true` + `b.hit.connect(_on_hit)` + rule fires → `SIGNAL_FIRED=true`. Note the GDScript-generator gotcha AGAIN (scene_accept redeclared `var before`/`var after` per rule → same "already declared" hang → fixed by declaring once, assigning per rule). Remaining 009: TC-7 lifecycle, TC-8 game trajectory, declarative JSON→tscn path.
- D25 (009 TC-7 lifecycle + TC-8 game trajectory, 2026-09-01): `scene_lifecycle.py` — engine-verified `TC8_END hp=0 dead=true signal=3` (3 hits kill the player, `hit` signal fires 3×) + `RESULT DONE` (add/free an extra entity, then free the whole scene → no ObjectDB leak warning, clean exit). **The 009 falsifiable scene bar TC-1..8 is now fully verified**: load/class/state/rule(1-4), cross-entity(5), signal(6), lifecycle(7), game-trajectory(8). The only genuinely-unverified piece is the DECLARATIVE `scene_from_json(JSON→tscn)` subsystem (S2) — unit-tested output but not engine-loaded, and it's a separate input path from the recipe→DLL-class scene scene_accept builds. LESSON (repeat): generated-GDScript `var` redeclaration bit a 3rd time (here `before`/`after` per rule) → always static-scan generated GDScript for dup `var` declarations before an engine run.
- D26 (declarative JSON→tscn path engine-verified, 2026-09-01): `decl_scene.json` (Bullet/Player + props) → `gdslc scene` → `emit_tscn` → a `[gd_scene format=3]` + nodes + connections → placed in an isolated project with the target_emit extension → engine load → `BUL_CLASS=Bullet`, `PRP_BUL_DMG=2`, `PRP_PLY_HP=10` — the declarative tscn's gdsl `type`+props instantiate & apply (Godot auto-loads `.gdextension` in the project, so a bare `type="ClassName"` resolves). This closes the last 009 gate. DESIGN BOUNDARY: declarative `connection{signal,from,to,method}` uses a GDScript-style `method` (`_on_hit`); gdsl classes expose only registered rule methods + signals, no arbitrary handlers — a connection to a gdsl class's method would fail (method doesn't exist); connections suit native/GDScript nodes, not gdsl classes.

## LLM convergence (010) — harness facts

- **Invoking `claude` from Python subprocess on Windows**: the npm global `claude` on PATH is a **`.cmd` shim** → `subprocess.run(["claude", ...], shell=False)` fails with `[WinError 2]` (CreateProcess can't run `.cmd`). Use the native exe: `%APPDATA%\npm\node_modules\@anthropic-ai\claude-code\bin\claude.exe`. The shim `claude` (bash) and `claude.cmd`/`claude.ps1` all exec that native `claude.exe`.
- **`claude -p` print mode**: `--allowedTools ''` (no tools) + `--max-turns 1` for pure generation; `--output-format json` → parse `.result` (text), `.total_cost_usd`, `.duration_ms`. Base system prompt is ~42K input tokens regardless of dir (even empty/no CLAUDE.md) → **~$0.20-0.21 per call** fixed. `--bare` needs `ANTHROPIC_API_KEY` (not set here). Authentication is OAuth (`claude auth status` → loggedIn).
- **Cold-test design**: pass GRAMMAR + few-shot examples + task all in the prompt (not via tools); run in an empty dir (no CLAUDE.md); validate output with the real compiler (`gdslc logic`). This measures "is the LANGUAGE LLM-friendly", not "is the LLM good at reading Godot source". Make the grammar reference self-sufficient (e.g., say string defaults use double quotes) or a no-example run will fail on quoting, not friendliness.

## Class name source (Gap B)

- **`extension_api.json` lives at the REPO ROOT** (`./extension_api.json`), NOT `core/extension/` — it is a `--dump-extension-api` artifact; `core/extension/` only has `gdextension_interface.json` (the ABI). It has `classes` (1036 ClassDB-registered classes, each with `name`/`inherits`/`is_instantiable`). This is the authoritative list for validating `@extends`.

## ABI walls (known, from source not guess)

- **No `string_destroy` / `string_name_destroy`** in the 4.7 ABI (grep `core/extension/gdextension_interface.json` `interface` → only `variant_destroy`/`object_destroy`). Any String/StringName value created via `string_new_with_utf8_chars`/`string_name_new_with_latin1_chars` cannot be released → refcount leak. `VariantTypeConstructor<String>` uses `memnew_placement` (copy-construct from source / placement-new into dest), so every Variant↔String marshal leaks one refcount ("transient String"). Bounded (one per reload per String field) and dev-time only. This is the reason String fields store as `char *` rather than a persistent `GDExtensionString` (which would leak per-object in production).

