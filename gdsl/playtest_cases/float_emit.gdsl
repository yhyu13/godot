type Player @extends CharacterBody2D
    signals: dash
    state:
        speed: float = 100.0
        hp: int = 3
    rule Dash by Player:
        when self.hp > 0
        then self.speed += 25, emit(dash)